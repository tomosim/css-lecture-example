import React from "react";

const Sidebar = () => {
  return (
    <div>
      <div>
        <h3>This is the (soon to be sidebar) navigator</h3>
        <button>Toggle dark mode</button>
        <button>Some other link</button>
        <button>One more link</button>
        <button>This is the last link</button>
      </div>
    </div>
  );
};

export default Sidebar;
